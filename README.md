# Cross Platform ONVIF Camera Manager
### Built with Avalonia C# .NET
## When running on Linux, installing LibVLC is required:
To install LibVLC, run this command in the Terminal: `sudo apt install vlc libvlc-dev`
